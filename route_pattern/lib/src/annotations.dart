
/// An annotation used to specify that a string constant 
/// is a route pattern.
class RoutePattern {
  /// Creates a new [Pattern] instance.
  const RoutePattern();
}

/// Default [BuiltBloc] annotation.
const route = RoutePattern();